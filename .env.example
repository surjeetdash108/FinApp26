NEXT_PUBLIC_SENTRY_DSN=

# MarketCatalystBackEnd base URL. Local dev backend defaults to port 4100 if
# unset — override to point at a deployed Cloud Run URL.
NEXT_PUBLIC_BACKEND_URL=http://localhost:4100

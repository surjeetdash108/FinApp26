# Dev-only overrides. Next.js loads this ONLY for `next dev` (never for
# `next build`), unlike .env.local which loads for every command and would
# otherwise shadow .env.production's value in a production build.
NEXT_PUBLIC_BACKEND_URL=http://localhost:4100
